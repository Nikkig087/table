/*the ready function makes sure the dom is loaded before anything inside it runs*/


$(document).ready(function() {

    //Use jQuery to change CSS classes

    //$("h2").addClass("underline") // underline all h2 elements
    //$("ul").addClass("border") // adds a border to all lists

    // we can also do remove class
    //$("h2").removeClass("underline") // removes underline all h2 elements
    //$("ul").removeClass("border") // removes a border to all lists
});